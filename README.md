# Configuring for 3D models
Go to Preferences > Configure Paths..., and add `JERRYMK_KICAD_LIB` to the repository root for 3D models to work (model paths will be in ${JERRYMK_KICAD_LIB}/3d_models/... format)
